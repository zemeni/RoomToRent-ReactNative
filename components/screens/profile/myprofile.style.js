import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    myProfile: {
        flex: 1,
        justifyContent: "space-between",
        alignItems: "center",
    },
});
